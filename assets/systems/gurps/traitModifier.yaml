$schema: http://json-schema.org/draft-07/schema
$id: https://valorsheet/systems/gurps/traitModifier.yaml
type: object
properties:
  enabled:
    type: boolean
  cost:
    type: integer
  name: 
    type: string
  costType:
    enum:
      - percentage
      - points
      - multiplier
  levels:
    type: integer
  hasLevels:
    type: boolean
  affects:
    enum:
      - base only
      - levels only
      - total