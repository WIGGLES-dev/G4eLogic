# $schema: http://json-schema.org/draft-07/schema
# $id: http://valorsheet/systems/gurps/spell.yaml
allOf:
  - $ref: definitions.yaml#/skill
  - $ref: ../definitions.yaml#/resource
  - type: object
    properties:
      type:
        const: spell
        default: spell
      version:
        const: 1
      requiresConcentration:
        type: boolean
        default: false
      currentlyActive:
        type: boolean
        default: false
      college:
        type: string
      class:
        type: string
      resist:
        type: string
      powerSource:
        type: string
      spellClass:
        type: string
      castingCost:
        type: string
      maintenanceCost:
        type: string
      castingTime:
        type: string
      duration:
        type: string
      children:
        type: object
        properties:
          spell:
            type: array
            items:
              $ref: spell.yaml
