import { SkillBonus } from "@character/misc/feature";
import { AttributeBonus } from "@character/attribute";

export const FeatureClasses = {
    skillBonus: SkillBonus,
    attributeBonus: AttributeBonus
}