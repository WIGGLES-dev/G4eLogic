type: object
allOf:
  - $ref: ../definitions.yaml#/resource
  - $ref: definitions.yaml#/weapon
  - type: object
    properties:
      type:
        const: melee weapon
      version:
        const: 1
        default: 1
      parryBonus:
        type: integer
        default: 0
      blockBonus:
        type: integer
        default: 0
      reach:
        type: string
        default: "1"
